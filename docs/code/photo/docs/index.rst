Photo plugin
===============

The `photo` plugin performs aperture photometry on a source. That is, it produces an aperiodic light curve. This is in contrast to the `lc` plugin, which produces a light curve for a periodic source.