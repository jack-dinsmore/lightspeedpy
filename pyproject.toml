[project]
name = "lightspeedpy"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.21",
    "scipy>=1.13",
    "tqdm",
    "astropy>=6.1",
    "pint-pulsar>=1.1",
]

[tool.setuptools.packages.find]
where = ["python"]
