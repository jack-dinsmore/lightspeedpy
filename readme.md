To install this, I for installed a minimal example and then moved code in. The minimal example didn't have the data/ or test/ directories. Maybe those were the issue?

# Installation

```
maturin develop
python -m pip install -e .
```